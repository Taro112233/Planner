// Native multi-file Prisma schema entrypoint.
// Prisma reads every *.prisma file in this directory via prisma.config.ts.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}
